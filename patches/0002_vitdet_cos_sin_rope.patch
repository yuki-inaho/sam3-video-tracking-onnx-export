/home/inaho-omen/Project/sam3/sam3/model/vitdet.py → /home/inaho-omen/Project/sam3_onnx_sandbox/outputs/sam3_equiv_source/sam3/model/vitdet.py
   +752 added, -701 removed, ~76 modified

-  93 def window_partition(x: Tensor, window_size: int) -> Tuple[Tensor, Tuple[int, int]]:
+  93 def apply_rotary_enc2(
-  94     """
+  94     xq: torch.Tensor,
-  95     Partition into non-overlapping windows with padding if needed.
+  95     xk: torch.Tensor,
-  96     Args:
+  96     freqs_cos: torch.Tensor,
-  97         x (tensor): input tokens with [B, H, W, C].
+  97     freqs_sin: torch.Tensor,
-  98         window_size (int): window size.
+  98     repeat_freqs_k: bool = False,
-  99     Returns:
+  99 ) -> Tuple[torch.Tensor, torch.Tensor]:
- 100         windows: windows after partition with [B * num_windows, window_size, window_size, C].
+ 100     """ONNX-safe cos/sin RoPE equivalent to apply_rotary_enc (complex).
- 101         (Hp, Wp): padded height and width before partition
+ 101 
- 102     """
+ 102     Replaces torch.polar / view_as_complex / view_as_real with real-valued
- 103     B, H, W, C = x.shape
+ 103     arithmetic.  freqs_cos = freqs_cis.real, freqs_sin = freqs_cis.imag.
- 104 
+ 104     """
- 105     pad_h = (window_size - H % window_size) % window_size
+ 105     xq_reshaped = xq.view(*xq.shape[:-1], -1, 2)
- 106     pad_w = (window_size - W % window_size) % window_size
+ 106     xq_cos = xq_reshaped[..., 0]
- 107     if pad_h > 0 or pad_w > 0:
+ 107     xq_sin = xq_reshaped[..., 1]
- 108         x = F.pad(x, (0, 0, 0, pad_w, 0, pad_h))
+ 108 
- 109     Hp, Wp = H + pad_h, W + pad_w
+ 109     xk_reshaped = xk.view(*xk.shape[:-1], -1, 2)
- 110 
+ 110     xk_cos = xk_reshaped[..., 0]
- 111     x = x.view(B, Hp // window_size, window_size, Wp // window_size, window_size, C)
+ 111     xk_sin = xk_reshaped[..., 1]
- 112     windows = x.permute(0, 1, 3, 2, 4, 5).reshape(-1, window_size, window_size, C)
+ 112 
- 113     return windows, (Hp, Wp)
+ 113     freqs_cos = freqs_cos[None, None]
- 114 
+ 114     freqs_sin = freqs_sin[None, None]
- 116 def window_unpartition(
+ 116     if repeat_freqs_k:
- 117     windows: Tensor, window_size: int, pad_hw: Tuple[int, int], hw: Tuple[int, int]
+ 117         r = xk_reshaped.shape[-3] // xq_reshaped.shape[-3]
- 118 ) -> Tensor:
+ 118         freqs_cos = freqs_cos.repeat(*([1] * (freqs_cos.ndim - 2)), r, 1)
- 119     """
+ 119         freqs_sin = freqs_sin.repeat(*([1] * (freqs_sin.ndim - 2)), r, 1)
- 120     Window unpartition into original sequences and removing padding.
+ 120 
- 121     Args:
+ 121     xq_out = torch.stack(
- 122         x (tensor): input tokens with [B * num_windows, window_size, window_size, C].
+ 122         [xq_cos * freqs_cos - xq_sin * freqs_sin,
- 123         window_size (int): window size.
+ 123          xq_cos * freqs_sin + xq_sin * freqs_cos],
- 124         pad_hw (Tuple): padded height and width (Hp, Wp).
+ 124         dim=-1,
- 125         hw (Tuple): original height and width (H, W) before padding.
+ 125     ).flatten(3)
- 126     Returns:
+ 126     xk_out = torch.stack(
- 127         x: unpartitioned sequences with [B, H, W, C].
+ 127         [xk_cos * freqs_cos - xk_sin * freqs_sin,
- 128     """
+ 128          xk_cos * freqs_sin + xk_sin * freqs_cos],
- 129     Hp, Wp = pad_hw
+ 129         dim=-1,
- 130     H, W = hw
+ 130     ).flatten(3)
- 131     B = windows.shape[0] // (Hp * Wp // window_size // window_size)
+ 131 
- 132     x = windows.reshape(
+ 132     return xq_out.type_as(xq), xk_out.type_as(xk)
- 133         B, Hp // window_size, Wp // window_size, window_size, window_size, -1
+ 133 
- 134     )
+ 134 
- 135     x = x.permute(0, 1, 3, 2, 4, 5).reshape(B, Hp, Wp, -1)
+ 135 def window_partition(x: Tensor, window_size: int) -> Tuple[Tensor, Tuple[int, int]]:
- 136 
+ 136     """
- 137     if Hp > H or Wp > W:
+ 137     Partition into non-overlapping windows with padding if needed.
- 138         x = x[:, :H, :W, :]
+ 138     Args:
- 139     return x
+ 139         x (tensor): input tokens with [B, H, W, C].
- 140 
+ 140         window_size (int): window size.
- 141 
+ 141     Returns:
- 142 def get_rel_pos(q_size: int, k_size: int, rel_pos: Tensor) -> Tensor:
+ 142         windows: windows after partition with [B * num_windows, window_size, window_size, C].
- 143     """
+ 143         (Hp, Wp): padded height and width before partition
- 144     Get relative positional embeddings according to the relative positions of
+ 144     """
- 145         query and key sizes.
+ 145     B, H, W, C = x.shape
- 146     Args:
+ 146 
- 147         q_size (int): size of query q.
+ 147     pad_h = (window_size - H % window_size) % window_size
- 148         k_size (int): size of key k.
+ 148     pad_w = (window_size - W % window_size) % window_size
- 149         rel_pos (Tensor): relative position embeddings (L, C).
+ 149     if pad_h > 0 or pad_w > 0:
- 150     Returns:
+ 150         x = F.pad(x, (0, 0, 0, pad_w, 0, pad_h))
- 151         Extracted positional embeddings according to relative positions.
+ 151     Hp, Wp = H + pad_h, W + pad_w
- 152     """
+ 152 
- 153     max_rel_dist = int(2 * max(q_size, k_size) - 1)
+ 153     x = x.view(B, Hp // window_size, window_size, Wp // window_size, window_size, C)
- 154     # Interpolate rel pos if needed.
+ 154     windows = x.permute(0, 1, 3, 2, 4, 5).reshape(-1, window_size, window_size, C)
- 155     if rel_pos.shape[0] != max_rel_dist:
+ 155     return windows, (Hp, Wp)
- 156         # Interpolate rel pos.
+ 156 
- 157         rel_pos_resized = F.interpolate(
+ 157 
- 158             rel_pos.reshape(1, rel_pos.shape[0], -1).permute(0, 2, 1),
+ 158 def window_unpartition(
- 159             size=max_rel_dist,
+ 159     windows: Tensor, window_size: int, pad_hw: Tuple[int, int], hw: Tuple[int, int]
- 160             mode="linear",
+ 160 ) -> Tensor:
- 161             align_corners=False,
+ 161     """
- 162         )
+ 162     Window unpartition into original sequences and removing padding.
- 163         rel_pos_resized = rel_pos_resized.reshape(-1, max_rel_dist).permute(1, 0)
+ 163     Args:
- 164     else:
+ 164         x (tensor): input tokens with [B * num_windows, window_size, window_size, C].
- 165         rel_pos_resized = rel_pos
+ 165         window_size (int): window size.
- 166 
+ 166         pad_hw (Tuple): padded height and width (Hp, Wp).
~ 167     # Scale the coords with short length if shapes for q and k are different. →         hw (Tuple): original height and width (H, W) before padding.
- 168     q_coords = torch.arange(q_size)[:, None] * max(k_size / q_size, 1.0)
+ 168     Returns:
- 169     k_coords = torch.arange(k_size)[None, :] * max(q_size / k_size, 1.0)
+ 169         x: unpartitioned sequences with [B, H, W, C].
- 170     relative_coords = (q_coords - k_coords) + (k_size - 1) * max(q_size / k_size, 1.0)
+ 170     """
- 171 
+ 171     Hp, Wp = pad_hw
- 172     return rel_pos_resized[relative_coords.long()]
+ 172     H, W = hw
- 173 
+ 173     B = windows.shape[0] // (Hp * Wp // window_size // window_size)
- 174 
+ 174     x = windows.reshape(
- 175 def get_abs_pos(
+ 175         B, Hp // window_size, Wp // window_size, window_size, window_size, -1
- 176     abs_pos: Tensor,
+ 176     )
- 177     has_cls_token: bool,
+ 177     x = x.permute(0, 1, 3, 2, 4, 5).reshape(B, Hp, Wp, -1)
- 178     hw: Tuple[int, int],
+ 178 
- 179     retain_cls_token: bool = False,
+ 179     if Hp > H or Wp > W:
- 180     tiling: bool = False,
+ 180         x = x[:, :H, :W, :]
- 181 ) -> Tensor:
+ 181     return x
- 182     """
+ 182 
- 183     Calculate absolute positional embeddings. If needed, resize embeddings and remove cls_token
+ 183 
- 184         dimension for the original embeddings.
+ 184 def get_rel_pos(q_size: int, k_size: int, rel_pos: Tensor) -> Tensor:
- 185     Args:
+ 185     """
~ 186         abs_pos (Tensor): absolute positional embeddings with (1, num_position, C). →     Get relative positional embeddings according to the relative positions of
- 187         has_cls_token (bool): If true, has 1 embedding in abs_pos for cls token.
+ 187         query and key sizes.
- 188         hw (Tuple): size of input image tokens.
+ 188     Args:
- 189         retain_cls_token: whether to retain the cls_token
+ 189         q_size (int): size of query q.
- 190         tiling: whether to tile the embeddings, *instead* of interpolation (a la abs_win)
+ 190         k_size (int): size of key k.
- 191     Returns:
+ 191         rel_pos (Tensor): relative position embeddings (L, C).
- 192         Absolute positional embeddings after processing with shape (1, H, W, C),
+ 192     Returns:
- 193         if retain_cls_token is False, otherwise (1, 1+H*W, C)
+ 193         Extracted positional embeddings according to relative positions.
- 195     if retain_cls_token:
+ 195     max_rel_dist = int(2 * max(q_size, k_size) - 1)
- 196         assert has_cls_token
+ 196     # Interpolate rel pos if needed.
- 197 
+ 197     if rel_pos.shape[0] != max_rel_dist:
- 198     h, w = hw
+ 198         # Interpolate rel pos.
- 199     if has_cls_token:
+ 199         rel_pos_resized = F.interpolate(
- 200         cls_pos = abs_pos[:, :1]
+ 200             rel_pos.reshape(1, rel_pos.shape[0], -1).permute(0, 2, 1),
- 201         abs_pos = abs_pos[:, 1:]
+ 201             size=max_rel_dist,
- 202 
+ 202             mode="linear",
- 203     xy_num = abs_pos.shape[1]
+ 203             align_corners=False,
- 204     size = int(math.sqrt(xy_num))
+ 204         )
- 205     assert size * size == xy_num
+ 205         rel_pos_resized = rel_pos_resized.reshape(-1, max_rel_dist).permute(1, 0)
- 206 
+ 206     else:
- 207     if size != h or size != w:
+ 207         rel_pos_resized = rel_pos
- 208         new_abs_pos = abs_pos.reshape(1, size, size, -1).permute(0, 3, 1, 2)
+ 208 
- 209         if tiling:
+ 209     # Scale the coords with short length if shapes for q and k are different.
- 210             new_abs_pos = new_abs_pos.tile(
+ 210     q_coords = torch.arange(q_size)[:, None] * max(k_size / q_size, 1.0)
~ 211                 [1, 1] + [x // y + 1 for x, y in zip((h, w), new_abs_pos.shape[2:])] →     k_coords = torch.arange(k_size)[None, :] * max(q_size / k_size, 1.0)
- 212             )[:, :, :h, :w]
+ 212     relative_coords = (q_coords - k_coords) + (k_size - 1) * max(q_size / k_size, 1.0)
- 213         else:
+ 213 
~ 214             new_abs_pos = F.interpolate( →     return rel_pos_resized[relative_coords.long()]
- 215                 new_abs_pos,
+ 215 
- 216                 size=(h, w),
+ 216 
- 217                 mode="bicubic",
+ 217 def get_abs_pos(
- 218                 align_corners=False,
+ 218     abs_pos: Tensor,
- 219             )
+ 219     has_cls_token: bool,
- 220 
+ 220     hw: Tuple[int, int],
~ 221         if not retain_cls_token: →     retain_cls_token: bool = False,
- 222             return new_abs_pos.permute(0, 2, 3, 1)
+ 222     tiling: bool = False,
- 223         else:
+ 223 ) -> Tensor:
- 224             # add cls_token back, flatten spatial dims
+ 224     """
- 225             assert has_cls_token
+ 225     Calculate absolute positional embeddings. If needed, resize embeddings and remove cls_token
- 226             return torch.cat(
+ 226         dimension for the original embeddings.
- 227                 [cls_pos, new_abs_pos.permute(0, 2, 3, 1).reshape(1, h * w, -1)],
+ 227     Args:
- 228                 dim=1,
+ 228         abs_pos (Tensor): absolute positional embeddings with (1, num_position, C).
- 229             )
+ 229         has_cls_token (bool): If true, has 1 embedding in abs_pos for cls token.
- 230 
+ 230         hw (Tuple): size of input image tokens.
- 231     else:
+ 231         retain_cls_token: whether to retain the cls_token
- 232         if not retain_cls_token:
+ 232         tiling: whether to tile the embeddings, *instead* of interpolation (a la abs_win)
- 233             return abs_pos.reshape(1, h, w, -1)
+ 233     Returns:
- 234         else:
+ 234         Absolute positional embeddings after processing with shape (1, H, W, C),
- 235             assert has_cls_token
+ 235         if retain_cls_token is False, otherwise (1, 1+H*W, C)
- 236             return torch.cat([cls_pos, abs_pos], dim=1)
+ 236     """
- 237 
+ 237     if retain_cls_token:
- 238 
+ 238         assert has_cls_token
- 239 def concat_rel_pos(
+ 239 
- 240     q: Tensor,
+ 240     h, w = hw
- 241     k: Tensor,
+ 241     if has_cls_token:
- 242     q_hw: Tuple[int, int],
+ 242         cls_pos = abs_pos[:, :1]
- 243     k_hw: Tuple[int, int],
+ 243         abs_pos = abs_pos[:, 1:]
- 244     rel_pos_h: Tensor,
+ 244 
- 245     rel_pos_w: Tensor,
+ 245     xy_num = abs_pos.shape[1]
- 246     rescale: bool = False,
+ 246     size = int(math.sqrt(xy_num))
- 247     relative_coords: Optional[Tensor] = None,
+ 247     assert size * size == xy_num
- 248 ) -> Tuple[Tensor, Tensor]:
+ 248 
- 249     """
+ 249     if size != h or size != w:
- 250     Concatenate rel pos coeffs to the q & k tensors, so that qk^T is now
+ 250         new_abs_pos = abs_pos.reshape(1, size, size, -1).permute(0, 3, 1, 2)
- 251     effectively including rel pos biases.
+ 251         if tiling:
- 252     Args:
+ 252             new_abs_pos = new_abs_pos.tile(
- 253         q (Tensor): q tensor with shape (B, L_q, C).
+ 253                 [1, 1] + [x // y + 1 for x, y in zip((h, w), new_abs_pos.shape[2:])]
- 254         k (Tensor): k tensor with shape (B, L_k, C).
+ 254             )[:, :, :h, :w]
- 255         q_hw, k_hw: These are spatial size of q & k tensors.
+ 255         else:
~ 256         rel_pos_h, rel_pos_w: These are relative pos embeddings/params of height, width. →             new_abs_pos = F.interpolate(
- 257         rescale (bool): whether to rescale. e.g. for use when using sdpa, pytorch will
+ 257                 new_abs_pos,
- 258             scale by the wrong factor due to the concat.
+ 258                 size=(h, w),
- 259     Returns:
+ 259                 mode="bicubic",
- 260         q, k: But, padded so that qk^T accounts for rel pos biases
+ 260                 align_corners=False,
- 261     """
+ 261             )
- 262     q_h, q_w = q_hw
+ 262 
- 263     k_h, k_w = k_hw
+ 263         if not retain_cls_token:
- 264 
+ 264             return new_abs_pos.permute(0, 2, 3, 1)
- 265     assert (q_h == q_w) and (k_h == k_w), "only square inputs supported"
+ 265         else:
- 266 
+ 266             # add cls_token back, flatten spatial dims
~ 267     if relative_coords is not None: →             assert has_cls_token
- 268         Rh = rel_pos_h[relative_coords]
+ 268             return torch.cat(
- 269         Rw = rel_pos_w[relative_coords]
+ 269                 [cls_pos, new_abs_pos.permute(0, 2, 3, 1).reshape(1, h * w, -1)],
- 270     else:
+ 270                 dim=1,
- 271         Rh = get_rel_pos(q_h, k_h, rel_pos_h)
+ 271             )
- 272         Rw = get_rel_pos(q_w, k_w, rel_pos_w)
+ 272 
- 273 
+ 273     else:
- 274     B, _, dim = q.shape
+ 274         if not retain_cls_token:
- 275     r_q = q.reshape(B, q_h, q_w, dim)
+ 275             return abs_pos.reshape(1, h, w, -1)
- 276 
+ 276         else:
- 277     old_scale = dim**0.5
+ 277             assert has_cls_token
~ 278     new_scale = (dim + k_h + k_w) ** 0.5 if rescale else old_scale  # for sdpa →             return torch.cat([cls_pos, abs_pos], dim=1)
- 279     # attn will be divided by new_scale, but we want to divide q by old_scale
+ 279 
- 280     scale_ratio = new_scale / old_scale
+ 280 
- 281 
+ 281 def concat_rel_pos(
- 282     rel_h = torch.einsum("bhwc,hkc->bhwk", r_q, Rh) * new_scale  # (B, q_h, q_w, k_h)
+ 282     q: Tensor,
- 283     rel_w = torch.einsum("bhwc,wkc->bhwk", r_q, Rw) * new_scale  # (B, q_h, q_w, k_w)
+ 283     k: Tensor,
- 284 
+ 284     q_hw: Tuple[int, int],
- 285     eye_h = torch.eye(k_h, dtype=q.dtype, device=q.device)
+ 285     k_hw: Tuple[int, int],
- 286     eye_w = torch.eye(k_w, dtype=q.dtype, device=q.device)
+ 286     rel_pos_h: Tensor,
- 287 
+ 287     rel_pos_w: Tensor,
- 288     eye_h = eye_h.view(1, k_h, 1, k_h).expand([B, k_h, k_w, k_h])
+ 288     rescale: bool = False,
- 289     eye_w = eye_w.view(1, 1, k_w, k_w).expand([B, k_h, k_w, k_w])
+ 289     relative_coords: Optional[Tensor] = None,
- 290 
+ 290 ) -> Tuple[Tensor, Tensor]:
- 291     q = torch.cat([r_q * scale_ratio, rel_h, rel_w], dim=-1).view(B, q_h * q_w, -1)
+ 291     """
- 292     k = torch.cat([k.view(B, k_h, k_w, -1), eye_h, eye_w], dim=-1).view(
+ 292     Concatenate rel pos coeffs to the q & k tensors, so that qk^T is now
- 293         B, k_h * k_w, -1
+ 293     effectively including rel pos biases.
- 294     )
+ 294     Args:
- 295 
+ 295         q (Tensor): q tensor with shape (B, L_q, C).
- 296     return q, k
+ 296         k (Tensor): k tensor with shape (B, L_k, C).
- 297 
+ 297         q_hw, k_hw: These are spatial size of q & k tensors.
- 298 
+ 298         rel_pos_h, rel_pos_w: These are relative pos embeddings/params of height, width.
~ 299 class PatchEmbed(nn.Module): →         rescale (bool): whether to rescale. e.g. for use when using sdpa, pytorch will
- 300     """
+ 300             scale by the wrong factor due to the concat.
- 301     Image to Patch Embedding.
+ 301     Returns:
- 302     """
+ 302         q, k: But, padded so that qk^T accounts for rel pos biases
- 303 
+ 303     """
- 304     def __init__(
+ 304     q_h, q_w = q_hw
- 305         self,
+ 305     k_h, k_w = k_hw
- 306         kernel_size: Tuple[int, int] = (16, 16),
+ 306 
- 307         stride: Tuple[int, int] = (16, 16),
+ 307     assert (q_h == q_w) and (k_h == k_w), "only square inputs supported"
- 308         padding: Tuple[int, int] = (0, 0),
+ 308 
- 309         in_chans: int = 3,
+ 309     if relative_coords is not None:
- 310         embed_dim: int = 768,
+ 310         Rh = rel_pos_h[relative_coords]
- 311         bias: bool = True,
+ 311         Rw = rel_pos_w[relative_coords]
- 312     ):
+ 312     else:
- 313         """
+ 313         Rh = get_rel_pos(q_h, k_h, rel_pos_h)
- 314         Args:
+ 314         Rw = get_rel_pos(q_w, k_w, rel_pos_w)
- 315             kernel_size (Tuple): kernel size of the projection layer.
+ 315 
- 316             stride (Tuple): stride of the projection layer.
+ 316     B, _, dim = q.shape
- 317             padding (Tuple): padding size of the projection layer.
+ 317     r_q = q.reshape(B, q_h, q_w, dim)
- 318             in_chans (int): Number of input image channels.
+ 318 
- 319             embed_dim (int):  embed_dim (int): Patch embedding dimension.
+ 319     old_scale = dim**0.5
- 320         """
+ 320     new_scale = (dim + k_h + k_w) ** 0.5 if rescale else old_scale  # for sdpa
- 321         super().__init__()
+ 321     # attn will be divided by new_scale, but we want to divide q by old_scale
- 322 
+ 322     scale_ratio = new_scale / old_scale
- 323         self.proj = nn.Conv2d(
+ 323 
- 324             in_chans,
+ 324     rel_h = torch.einsum("bhwc,hkc->bhwk", r_q, Rh) * new_scale  # (B, q_h, q_w, k_h)
- 325             embed_dim,
+ 325     rel_w = torch.einsum("bhwc,wkc->bhwk", r_q, Rw) * new_scale  # (B, q_h, q_w, k_w)
- 326             kernel_size=kernel_size,
+ 326 
- 327             stride=stride,
+ 327     eye_h = torch.eye(k_h, dtype=q.dtype, device=q.device)
- 328             padding=padding,
+ 328     eye_w = torch.eye(k_w, dtype=q.dtype, device=q.device)
- 329             bias=bias,
+ 329 
- 330         )
+ 330     eye_h = eye_h.view(1, k_h, 1, k_h).expand([B, k_h, k_w, k_h])
- 331 
+ 331     eye_w = eye_w.view(1, 1, k_w, k_w).expand([B, k_h, k_w, k_w])
- 332     def forward(self, x: Tensor) -> Tensor:
+ 332 
- 333         x = self.proj(x)
+ 333     q = torch.cat([r_q * scale_ratio, rel_h, rel_w], dim=-1).view(B, q_h * q_w, -1)
- 334         # B C H W -> B H W C
+ 334     k = torch.cat([k.view(B, k_h, k_w, -1), eye_h, eye_w], dim=-1).view(
- 335         x = x.permute(0, 2, 3, 1)
+ 335         B, k_h * k_w, -1
- 336         return x
+ 336     )
- 338 
+ 338     return q, k
- 339 class Attention(nn.Module):
+ 339 
- 340     """Multi-head Attention block with relative position embeddings and 2d-rope."""
+ 340 
- 341 
+ 341 class PatchEmbed(nn.Module):
- 342     def __init__(
+ 342     """
- 343         self,
+ 343     Image to Patch Embedding.
- 344         dim: int,
+ 344     """
- 345         num_heads: int = 8,
+ 345 
- 346         qkv_bias: bool = True,
+ 346     def __init__(
- 347         use_rel_pos: bool = False,
+ 347         self,
~ 348         rel_pos_zero_init: bool = True, →         kernel_size: Tuple[int, int] = (16, 16),
~ 349         input_size: Optional[Tuple[int, int]] = None, →         stride: Tuple[int, int] = (16, 16),
- 350         cls_token: bool = False,
+ 350         padding: Tuple[int, int] = (0, 0),
- 351         use_rope: bool = False,
+ 351         in_chans: int = 3,
- 352         rope_theta: float = 10000.0,
+ 352         embed_dim: int = 768,
~ 353         rope_pt_size: Optional[Tuple[int, int]] = None, →         bias: bool = True,
- 354         rope_interp: bool = False,
+ 354     ):
- 355     ):
+ 355         """
- 356         """
+ 356         Args:
- 357         Args:
+ 357             kernel_size (Tuple): kernel size of the projection layer.
~ 358             dim (int): Number of input channels. →             stride (Tuple): stride of the projection layer.
~ 359             num_heads (int): Number of attention heads. →             padding (Tuple): padding size of the projection layer.
~ 360             qkv_bias (bool:  If True, add a learnable bias to query, key, value. →             in_chans (int): Number of input image channels.
~ 361             rel_pos (bool): If True, add relative positional embeddings to the attention map. →             embed_dim (int):  embed_dim (int): Patch embedding dimension.
- 362             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters.
+ 362         """
- 363             input_size (int or None): Input resolution for calculating the relative positional
+ 363         super().__init__()
- 364                 parameter size or rope size.
+ 364 
- 365             attn_type: Type of attention operation, e.g. "vanilla", "vanilla-xformer".
+ 365         self.proj = nn.Conv2d(
- 366             cls_token: whether a cls_token is present.
+ 366             in_chans,
- 367             use_rope: whether to use rope 2d (indep of use_rel_pos, as it can be used together)
+ 367             embed_dim,
- 368             rope_theta: control frequencies of rope
+ 368             kernel_size=kernel_size,
- 369             rope_pt_size: size of rope in previous stage of training, needed for interpolation or tiling
+ 369             stride=stride,
- 370             rope_interp: whether to interpolate (or extrapolate) rope to match input size
+ 370             padding=padding,
- 371         """
+ 371             bias=bias,
- 372         super().__init__()
+ 372         )
- 373         self.num_heads = num_heads
+ 373 
- 374         self.head_dim = dim // num_heads
+ 374     def forward(self, x: Tensor) -> Tensor:
- 375         self.scale = self.head_dim**-0.5
+ 375         x = self.proj(x)
- 376         self.cls_token = cls_token
+ 376         # B C H W -> B H W C
- 377 
+ 377         x = x.permute(0, 2, 3, 1)
- 378         self.qkv = nn.Linear(dim, dim * 3, bias=qkv_bias)
+ 378         return x
- 379         self.proj = nn.Linear(dim, dim)
+ 379 
- 381         # rel_pos embeddings and rope
+ 381 class Attention(nn.Module):
- 382         self.use_rel_pos = use_rel_pos
+ 382     """Multi-head Attention block with relative position embeddings and 2d-rope."""
- 383         self.input_size = input_size
+ 383 
- 384 
+ 384     def __init__(
- 385         self.use_rope = use_rope
+ 385         self,
- 386         self.rope_theta = rope_theta
+ 386         dim: int,
- 387         self.rope_pt_size = rope_pt_size
+ 387         num_heads: int = 8,
- 388         self.rope_interp = rope_interp
+ 388         qkv_bias: bool = True,
- 389 
+ 389         use_rel_pos: bool = False,
~ 390         # init rel_pos embeddings and rope →         rel_pos_zero_init: bool = True,
- 391         self._setup_rel_pos(rel_pos_zero_init)
+ 391         input_size: Optional[Tuple[int, int]] = None,
- 392         self._setup_rope_freqs()
+ 392         cls_token: bool = False,
- 393 
+ 393         use_rope: bool = False,
- 394     def _setup_rel_pos(self, rel_pos_zero_init: bool = True) -> None:
+ 394         rope_theta: float = 10000.0,
~ 395         if not self.use_rel_pos: →         rope_pt_size: Optional[Tuple[int, int]] = None,
- 396             self.rel_pos_h = None
+ 396         rope_interp: bool = False,
- 397             self.rel_pos_w = None
+ 397     ):
- 398             return
+ 398         """
- 399 
+ 399         Args:
~ 400         assert self.input_size is not None →             dim (int): Number of input channels.
- 401         assert self.cls_token is False, "not supported"
+ 401             num_heads (int): Number of attention heads.
- 402         # initialize relative positional embeddings
+ 402             qkv_bias (bool:  If True, add a learnable bias to query, key, value.
~ 403         self.rel_pos_h = nn.Parameter( →             rel_pos (bool): If True, add relative positional embeddings to the attention map.
~ 404             torch.zeros(2 * self.input_size[0] - 1, self.head_dim) →             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters.
- 405         )
+ 405             input_size (int or None): Input resolution for calculating the relative positional
- 406         self.rel_pos_w = nn.Parameter(
+ 406                 parameter size or rope size.
- 407             torch.zeros(2 * self.input_size[1] - 1, self.head_dim)
+ 407             attn_type: Type of attention operation, e.g. "vanilla", "vanilla-xformer".
- 408         )
+ 408             cls_token: whether a cls_token is present.
- 409 
+ 409             use_rope: whether to use rope 2d (indep of use_rel_pos, as it can be used together)
~ 410         if not rel_pos_zero_init: →             rope_theta: control frequencies of rope
~ 411             trunc_normal_(self.rel_pos_h, std=0.02) →             rope_pt_size: size of rope in previous stage of training, needed for interpolation or tiling
~ 412             trunc_normal_(self.rel_pos_w, std=0.02) →             rope_interp: whether to interpolate (or extrapolate) rope to match input size
- 413 
+ 413         """
- 414         # Precompute the relative coords
+ 414         super().__init__()
- 415         H, W = self.input_size
+ 415         self.num_heads = num_heads
- 416         q_coords = torch.arange(H)[:, None]
+ 416         self.head_dim = dim // num_heads
- 417         k_coords = torch.arange(W)[None, :]
+ 417         self.scale = self.head_dim**-0.5
- 418         relative_coords = (q_coords - k_coords) + (H - 1)
+ 418         self.cls_token = cls_token
- 419         self.register_buffer("relative_coords", relative_coords.long())
+ 419 
- 420 
+ 420         self.qkv = nn.Linear(dim, dim * 3, bias=qkv_bias)
- 421     def _setup_rope_freqs(self) -> None:
+ 421         self.proj = nn.Linear(dim, dim)
- 422         if not self.use_rope:
+ 422 
- 423             self.freqs_cis = None
+ 423         # rel_pos embeddings and rope
- 424             return
+ 424         self.use_rel_pos = use_rel_pos
- 425 
+ 425         self.input_size = input_size
- 426         assert self.input_size is not None
+ 426 
- 427         # determine rope input size
+ 427         self.use_rope = use_rope
~ 428         if self.rope_pt_size is None: →         self.rope_theta = rope_theta
~ 429             self.rope_pt_size = self.input_size →         self.rope_pt_size = rope_pt_size
- 430 
+ 430         self.rope_interp = rope_interp
- 431         # initialize 2d rope freqs
+ 431 
~ 432         self.compute_cis = partial( →         # init rel_pos embeddings and rope
- 433             compute_axial_cis,
+ 433         self._setup_rel_pos(rel_pos_zero_init)
- 434             dim=self.head_dim,
+ 434         self._setup_rope_freqs()
- 435             theta=self.rope_theta,
+ 435 
- 436         )
+ 436     def _setup_rel_pos(self, rel_pos_zero_init: bool = True) -> None:
- 437 
+ 437         if not self.use_rel_pos:
- 438         # interpolate rope
+ 438             self.rel_pos_h = None
- 439         scale_pos = 1.0
+ 439             self.rel_pos_w = None
- 440         if self.rope_interp:
+ 440             return
- 441             scale_pos = self.rope_pt_size[0] / self.input_size[0]
+ 441 
- 442         # get scaled freqs_cis
+ 442         assert self.input_size is not None
~ 443         freqs_cis = self.compute_cis( →         assert self.cls_token is False, "not supported"
- 444             end_x=self.input_size[0],
+ 444         # initialize relative positional embeddings
- 445             end_y=self.input_size[1],
+ 445         self.rel_pos_h = nn.Parameter(
- 446             scale_pos=scale_pos,
+ 446             torch.zeros(2 * self.input_size[0] - 1, self.head_dim)
- 448         if self.cls_token:
+ 448         self.rel_pos_w = nn.Parameter(
- 449             t = torch.zeros(
+ 449             torch.zeros(2 * self.input_size[1] - 1, self.head_dim)
- 450                 self.head_dim // 2,
+ 450         )
- 451                 dtype=torch.float32,
+ 451 
- 452                 device=freqs_cis.device,
+ 452         if not rel_pos_zero_init:
- 453             )
+ 453             trunc_normal_(self.rel_pos_h, std=0.02)
~ 454             cls_freqs_cis = torch.polar(torch.ones_like(t), t)[None, :] →             trunc_normal_(self.rel_pos_w, std=0.02)
- 455             freqs_cis = torch.cat([cls_freqs_cis, freqs_cis], dim=0)
+ 455 
- 456 
+ 456         # Precompute the relative coords
- 457         self.register_buffer("freqs_cis", freqs_cis)
+ 457         H, W = self.input_size
- 458 
+ 458         q_coords = torch.arange(H)[:, None]
- 459     def _apply_rope(self, q, k) -> Tuple[Tensor, Tensor]:
+ 459         k_coords = torch.arange(W)[None, :]
- 460         if not self.use_rope:
+ 460         relative_coords = (q_coords - k_coords) + (H - 1)
- 461             return q, k
+ 461         self.register_buffer("relative_coords", relative_coords.long())
- 463         assert self.freqs_cis is not None
+ 463     def _setup_rope_freqs(self) -> None:
~ 464         return apply_rotary_enc(q, k, freqs_cis=self.freqs_cis) →         if not self.use_rope:
- 465 
+ 465             self.freqs_cis = None
- 466     def forward(self, x: Tensor) -> Tensor:
+ 466             return
- 467         s = 1 if self.cls_token else 0  # used to exclude cls_token
+ 467 
- 468         if x.ndim == 4:
+ 468         assert self.input_size is not None
- 469             B, H, W, _ = x.shape
+ 469         # determine rope input size
- 470             assert s == 0  # no cls_token
+ 470         if self.rope_pt_size is None:
- 471             L = H * W
+ 471             self.rope_pt_size = self.input_size
- 472             ndim = 4
+ 472 
- 473         else:
+ 473         # initialize 2d rope freqs
- 474             assert x.ndim == 3
+ 474         self.compute_cis = partial(
- 475             B, L, _ = x.shape
+ 475             compute_axial_cis,
- 476             ndim = 3
+ 476             dim=self.head_dim,
- 477             H = W = math.sqrt(L - s)
+ 477             theta=self.rope_theta,
- 478 
+ 478         )
- 479         # qkv with shape (3, B, nHead, L, C)
+ 479 
- 480         qkv = self.qkv(x).reshape(B, L, 3, self.num_heads, -1)
+ 480         # interpolate rope
- 481         # q, k, v with shape (B, nHead, L, C)
+ 481         scale_pos = 1.0
- 482         q, k, v = qkv.permute(2, 0, 3, 1, 4).unbind(0)
+ 482         if self.rope_interp:
- 483 
+ 483             scale_pos = self.rope_pt_size[0] / self.input_size[0]
- 484         # handle rope and rel pos embeddings
+ 484         # get scaled freqs_cis
~ 485         q, k = self._apply_rope(q, k) →         freqs_cis = self.compute_cis(
- 486         if self.use_rel_pos:
+ 486             end_x=self.input_size[0],
- 487             q, k = concat_rel_pos(
+ 487             end_y=self.input_size[1],
- 488                 q.flatten(0, 1),
+ 488             scale_pos=scale_pos,
- 489                 k.flatten(0, 1),
+ 489         )
- 490                 (H, W),
+ 490         if self.cls_token:
- 491                 x.shape[1:3],
+ 491             t = torch.zeros(
- 492                 self.rel_pos_h,
+ 492                 self.head_dim // 2,
- 493                 self.rel_pos_w,
+ 493                 dtype=torch.float32,
- 494                 rescale=True,
+ 494                 device=freqs_cis.device,
- 495                 relative_coords=self.relative_coords,
+ 495             )
- 496             )
+ 496             cls_freqs_cis = torch.polar(torch.ones_like(t), t)[None, :]
- 497 
+ 497             freqs_cis = torch.cat([cls_freqs_cis, freqs_cis], dim=0)
- 498             # sdpa expects [B, nheads, H*W, C] so we transpose back
+ 498 
- 499             q = q.reshape(B, self.num_heads, H * W, -1)
+ 499         self.register_buffer("freqs_cis", freqs_cis)
- 500             k = k.reshape(B, self.num_heads, H * W, -1)
+ 500 
- 501 
+ 501     def _apply_rope(self, q, k) -> Tuple[Tensor, Tensor]:
- 502         x = F.scaled_dot_product_attention(q, k, v)
+ 502         if not self.use_rope:
- 503 
+ 503             return q, k
- 504         if ndim == 4:
+ 504 
- 505             x = (
+ 505         if hasattr(self, "freqs_cis"):
- 506                 x.view(B, self.num_heads, H, W, -1)
+ 506             # Standard PyTorch path: complex RoPE (numerically equivalent to cos/sin).
- 507                 .permute(0, 2, 3, 1, 4)
+ 507             assert self.freqs_cis is not None
- 508                 .reshape(B, H, W, -1)
+ 508             return apply_rotary_enc(q, k, freqs_cis=self.freqs_cis)
- 509             )
+ 509         # ONNX-export path: cos/sin RoPE (no complex ops).
- 510         else:
+ 510         # Activated by replacing freqs_cis buffer with freqs_cos / freqs_sin.
- 511             x = x.view(B, self.num_heads, L, -1).permute(0, 2, 1, 3).reshape(B, L, -1)
+ 511         assert hasattr(self, "freqs_cos") and hasattr(self, "freqs_sin"), (
- 512 
+ 512             "_apply_rope: freqs_cis is absent but freqs_cos / freqs_sin are not "
- 513         x = self.proj(x)
+ 513             "registered.  Call model.replace_rope_freqs() before export."
- 514 
+ 514         )
- 515         return x
+ 515         return apply_rotary_enc2(q, k, self.freqs_cos, self.freqs_sin)
- 517 
+ 517     def forward(self, x: Tensor) -> Tensor:
- 518 class Block(nn.Module):
+ 518         s = 1 if self.cls_token else 0  # used to exclude cls_token
- 519     """Transformer blocks with support of window attention"""
+ 519         if x.ndim == 4:
- 520 
+ 520             B, H, W, _ = x.shape
- 521     def __init__(
+ 521             assert s == 0  # no cls_token
- 522         self,
+ 522             L = H * W
- 523         dim: int,
+ 523             ndim = 4
- 524         num_heads: int,
+ 524         else:
- 525         mlp_ratio: float = 4.0,
+ 525             assert x.ndim == 3
- 526         qkv_bias: bool = True,
+ 526             B, L, _ = x.shape
- 527         drop_path: float = 0.0,
+ 527             ndim = 3
- 528         norm_layer: Callable[..., nn.Module] = nn.LayerNorm,
+ 528             H = W = math.sqrt(L - s)
- 529         act_layer: Callable[..., nn.Module] = nn.GELU,
+ 529 
- 530         use_rel_pos: bool = False,
+ 530         # qkv with shape (3, B, nHead, L, C)
- 531         rel_pos_zero_init: bool = True,
+ 531         qkv = self.qkv(x).reshape(B, L, 3, self.num_heads, -1)
- 532         window_size: int = 0,
+ 532         # q, k, v with shape (B, nHead, L, C)
- 533         input_size: Optional[Tuple[int, int]] = None,
+ 533         q, k, v = qkv.permute(2, 0, 3, 1, 4).unbind(0)
- 534         use_rope: bool = False,
+ 534 
- 535         rope_pt_size: Optional[Tuple[int, int]] = None,
+ 535         # handle rope and rel pos embeddings
- 536         rope_tiled: bool = False,
+ 536         q, k = self._apply_rope(q, k)
- 537         rope_interp: bool = False,
+ 537         if self.use_rel_pos:
- 538         use_ve_rope: bool = False,
+ 538             q, k = concat_rel_pos(
- 539         cls_token: bool = False,
+ 539                 q.flatten(0, 1),
- 540         dropout: float = 0.0,
+ 540                 k.flatten(0, 1),
- 541         init_values: Optional[float] = None,
+ 541                 (H, W),
- 542     ):
+ 542                 x.shape[1:3],
- 543         """
+ 543                 self.rel_pos_h,
- 544         Args:
+ 544                 self.rel_pos_w,
- 545             dim (int): Number of input channels.
+ 545                 rescale=True,
- 546             num_heads (int): Number of attention heads in each ViT block.
+ 546                 relative_coords=self.relative_coords,
- 547             mlp_ratio (float): Ratio of mlp hidden dim to embedding dim.
+ 547             )
- 548             qkv_bias (bool): If True, add a learnable bias to query, key, value.
+ 548 
- 549             drop_path (float): Stochastic depth rate.
+ 549             # sdpa expects [B, nheads, H*W, C] so we transpose back
- 550             norm_layer (nn.Module): Normalization layer.
+ 550             q = q.reshape(B, self.num_heads, H * W, -1)
- 551             act_layer (nn.Module): Activation layer.
+ 551             k = k.reshape(B, self.num_heads, H * W, -1)
- 552             use_rel_pos (bool): If True, add relative positional embeddings to the attention map.
+ 552 
~ 553             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters. →         x = F.scaled_dot_product_attention(q, k, v)
- 554             window_size (int): Window size for window attention blocks. If it equals 0, then not
+ 554 
- 555                 use window attention.
+ 555         if ndim == 4:
- 556             input_size (int or None): Input resolution for calculating the relative positional
+ 556             x = (
- 557                 parameter size.
+ 557                 x.view(B, self.num_heads, H, W, -1)
- 558             dropout (float): Dropout rate.
+ 558                 .permute(0, 2, 3, 1, 4)
- 559             cls_token: whether a cls_token is present.
+ 559                 .reshape(B, H, W, -1)
- 560             use_rope: whether to use rope 2d (indep of use_rel_pos, as it can be used together)
+ 560             )
- 561             rope_pt_size: size of rope in previous stage of training, needed for interpolation or tiling
+ 561         else:
~ 562             rope_interp: whether to interpolate (or extrapolate) rope to match target input size, →             x = x.view(B, self.num_heads, L, -1).permute(0, 2, 1, 3).reshape(B, L, -1)
- 563                 expected to specify source size as rope_pt_size.
+ 563 
- 564         """
+ 564         x = self.proj(x)
- 565         super().__init__()
+ 565 
- 566         self.norm1 = norm_layer(dim)
+ 566         return x
- 567         self.attn = Attention(
+ 567 
- 568             dim,
+ 568 
- 569             num_heads=num_heads,
+ 569 class Block(nn.Module):
- 570             qkv_bias=qkv_bias,
+ 570     """Transformer blocks with support of window attention"""
- 571             use_rel_pos=use_rel_pos,
+ 571 
- 572             rel_pos_zero_init=rel_pos_zero_init,
+ 572     def __init__(
- 573             input_size=input_size if window_size == 0 else (window_size, window_size),
+ 573         self,
- 574             use_rope=use_rope,
+ 574         dim: int,
- 575             rope_pt_size=rope_pt_size,
+ 575         num_heads: int,
- 576             rope_interp=rope_interp,
+ 576         mlp_ratio: float = 4.0,
- 577             cls_token=cls_token,
+ 577         qkv_bias: bool = True,
- 578         )
+ 578         drop_path: float = 0.0,
- 579         self.ls1 = (
+ 579         norm_layer: Callable[..., nn.Module] = nn.LayerNorm,
- 580             LayerScale(dim, init_values=init_values) if init_values else nn.Identity()
+ 580         act_layer: Callable[..., nn.Module] = nn.GELU,
- 581         )
+ 581         use_rel_pos: bool = False,
- 582         self.drop_path = DropPath(drop_path) if drop_path > 0.0 else nn.Identity()
+ 582         rel_pos_zero_init: bool = True,
- 583 
+ 583         window_size: int = 0,
- 584         self.norm2 = norm_layer(dim)
+ 584         input_size: Optional[Tuple[int, int]] = None,
- 585         self.mlp = Mlp(
+ 585         use_rope: bool = False,
- 586             in_features=dim,
+ 586         rope_pt_size: Optional[Tuple[int, int]] = None,
~ 587             hidden_features=int(dim * mlp_ratio), →         rope_tiled: bool = False,
- 588             act_layer=act_layer,
+ 588         rope_interp: bool = False,
- 589             drop=(dropout, 0.0),
+ 589         use_ve_rope: bool = False,
- 590         )
+ 590         cls_token: bool = False,
- 591         self.ls2 = (
+ 591         dropout: float = 0.0,
- 592             LayerScale(dim, init_values=init_values) if init_values else nn.Identity()
+ 592         init_values: Optional[float] = None,
~ 593         ) →     ):
- 594         self.dropout = nn.Dropout(dropout)
+ 594         """
- 595         self.window_size = window_size
+ 595         Args:
- 596 
+ 596             dim (int): Number of input channels.
- 597     def forward(self, x: Tensor) -> Tensor:
+ 597             num_heads (int): Number of attention heads in each ViT block.
- 598         shortcut = x
+ 598             mlp_ratio (float): Ratio of mlp hidden dim to embedding dim.
- 599         x = self.norm1(x)
+ 599             qkv_bias (bool): If True, add a learnable bias to query, key, value.
- 600         # Window partition
+ 600             drop_path (float): Stochastic depth rate.
- 601         if self.window_size > 0:
+ 601             norm_layer (nn.Module): Normalization layer.
- 602             H, W = x.shape[1], x.shape[2]
+ 602             act_layer (nn.Module): Activation layer.
~ 603             x, pad_hw = window_partition(x, self.window_size) →             use_rel_pos (bool): If True, add relative positional embeddings to the attention map.
- 604 
+ 604             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters.
- 605         x = self.ls1(self.attn(x))
+ 605             window_size (int): Window size for window attention blocks. If it equals 0, then not
~ 606         # Reverse window partition →                 use window attention.
- 607         if self.window_size > 0:
+ 607             input_size (int or None): Input resolution for calculating the relative positional
- 608             x = window_unpartition(x, self.window_size, pad_hw, (H, W))
+ 608                 parameter size.
- 609 
+ 609             dropout (float): Dropout rate.
- 610         x = shortcut + self.dropout(self.drop_path(x))
+ 610             cls_token: whether a cls_token is present.
~ 611         x = x + self.dropout(self.drop_path(self.ls2(self.mlp(self.norm2(x))))) →             use_rope: whether to use rope 2d (indep of use_rel_pos, as it can be used together)
- 612 
+ 612             rope_pt_size: size of rope in previous stage of training, needed for interpolation or tiling
- 613         return x
+ 613             rope_interp: whether to interpolate (or extrapolate) rope to match target input size,
- 614 
+ 614                 expected to specify source size as rope_pt_size.
- 615 
+ 615         """
- 616 class ViT(nn.Module):
+ 616         super().__init__()
- 617     """
+ 617         self.norm1 = norm_layer(dim)
- 618     This module implements Vision Transformer (ViT) backbone in :paper:`vitdet`.
+ 618         self.attn = Attention(
- 619     "Exploring Plain Vision Transformer Backbones for Object Detection",
+ 619             dim,
- 620     https://arxiv.org/abs/2203.16527
+ 620             num_heads=num_heads,
- 621     """
+ 621             qkv_bias=qkv_bias,
- 622 
+ 622             use_rel_pos=use_rel_pos,
- 623     def __init__(
+ 623             rel_pos_zero_init=rel_pos_zero_init,
- 624         self,
+ 624             input_size=input_size if window_size == 0 else (window_size, window_size),
- 625         img_size: int = 1024,
+ 625             use_rope=use_rope,
~ 626         patch_size: int = 16, →             rope_pt_size=rope_pt_size,
- 627         in_chans: int = 3,
+ 627             rope_interp=rope_interp,
- 628         embed_dim: int = 768,
+ 628             cls_token=cls_token,
- 629         depth: int = 12,
+ 629         )
- 630         num_heads: int = 12,
+ 630         self.ls1 = (
- 631         mlp_ratio: float = 4.0,
+ 631             LayerScale(dim, init_values=init_values) if init_values else nn.Identity()
- 632         qkv_bias: bool = True,
+ 632         )
~ 633         drop_path_rate: float = 0.0, →         self.drop_path = DropPath(drop_path) if drop_path > 0.0 else nn.Identity()
- 634         norm_layer: Union[Callable[..., nn.Module], str] = "LayerNorm",
+ 634 
- 635         act_layer: Callable[..., nn.Module] = nn.GELU,
+ 635         self.norm2 = norm_layer(dim)
- 636         use_abs_pos: bool = True,
+ 636         self.mlp = Mlp(
~ 637         tile_abs_pos: bool = True, →             in_features=dim,
- 638         rel_pos_blocks: Union[Tuple[int, ...], bool] = (2, 5, 8, 11),
+ 638             hidden_features=int(dim * mlp_ratio),
- 639         rel_pos_zero_init: bool = True,
+ 639             act_layer=act_layer,
- 640         window_size: int = 14,
+ 640             drop=(dropout, 0.0),
- 641         global_att_blocks: Tuple[int, ...] = (2, 5, 8, 11),
+ 641         )
- 642         use_rope: bool = False,
+ 642         self.ls2 = (
- 643         rope_pt_size: Optional[int] = None,
+ 643             LayerScale(dim, init_values=init_values) if init_values else nn.Identity()
- 644         use_interp_rope: bool = False,
+ 644         )
- 645         pretrain_img_size: int = 224,
+ 645         self.dropout = nn.Dropout(dropout)
- 646         pretrain_use_cls_token: bool = True,
+ 646         self.window_size = window_size
- 647         retain_cls_token: bool = True,
+ 647 
- 648         dropout: float = 0.0,
+ 648     def forward(self, x: Tensor) -> Tensor:
- 649         return_interm_layers: bool = False,
+ 649         shortcut = x
- 650         init_values: Optional[float] = None,  # for layerscale
+ 650         x = self.norm1(x)
- 651         ln_pre: bool = False,
+ 651         # Window partition
- 652         ln_post: bool = False,
+ 652         if self.window_size > 0:
- 653         bias_patch_embed: bool = True,
+ 653             H, W = x.shape[1], x.shape[2]
- 654         compile_mode: Optional[str] = None,
+ 654             x, pad_hw = window_partition(x, self.window_size)
- 655         use_act_checkpoint: bool = True,
+ 655 
- 656     ):
+ 656         x = self.ls1(self.attn(x))
- 657         """
+ 657         # Reverse window partition
- 658         Args:
+ 658         if self.window_size > 0:
~ 659             img_size (int): Input image size. Only relevant for rel pos or rope. →             x = window_unpartition(x, self.window_size, pad_hw, (H, W))
- 660             patch_size (int): Patch size.
+ 660 
~ 661             in_chans (int): Number of input image channels. →         x = shortcut + self.dropout(self.drop_path(x))
- 662             embed_dim (int): Patch embedding dimension.
+ 662         x = x + self.dropout(self.drop_path(self.ls2(self.mlp(self.norm2(x)))))
- 663             depth (int): Depth of ViT.
+ 663 
- 664             num_heads (int): Number of attention heads in each ViT block.
+ 664         return x
- 665             mlp_ratio (float): Ratio of mlp hidden dim to embedding dim.
+ 665 
- 666             qkv_bias (bool): If True, add a learnable bias to query, key, value.
+ 666 
~ 667             drop_path_rate (float): Stochastic depth rate. → class ViT(nn.Module):
- 668             norm_layer (nn.Module): Normalization layer.
+ 668     """
~ 669             act_layer (nn.Module): Activation layer. →     This module implements Vision Transformer (ViT) backbone in :paper:`vitdet`.
- 670             use_abs_pos (bool): If True, use absolute positional embeddings.
+ 670     "Exploring Plain Vision Transformer Backbones for Object Detection",
- 671             tile_abs_pos (bool): If True, tile absolute positional embeddings instead of interpolation.
+ 671     https://arxiv.org/abs/2203.16527
- 672             rel_pos_blocks (list): Blocks which have rel pos embeddings.
+ 672     """
- 673             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters.
+ 673 
- 674             window_size (int): Window size for window attention blocks.
+ 674     def __init__(
- 675             global_att_blocks (list): Indexes for blocks using global attention (other blocks use window attention).
+ 675         self,
- 676             use_rope (bool): whether to use rope 2d (indep of rel_pos_blocks, as it can be used together).
+ 676         img_size: int = 1024,
- 677             rope_pt_size (int): size of rope in previous stage of training, needed for interpolation or tiling.
+ 677         patch_size: int = 16,
- 678             use_interp_rope: whether to interpolate (or extrapolate) rope to match target input size,
+ 678         in_chans: int = 3,
- 679                 expected to specify source size as rope_pt_size.
+ 679         embed_dim: int = 768,
- 680             use_act_checkpoint (bool): If True, use activation checkpointing.
+ 680         depth: int = 12,
- 681             pretrain_img_size (int): input image size for pretraining models.
+ 681         num_heads: int = 12,
- 682             pretrain_use_cls_token (bool): If True, pretraining models use class token.
+ 682         mlp_ratio: float = 4.0,
~ 683             retain_cls_token: whether cls_token should be retained. →         qkv_bias: bool = True,
- 684             dropout (float): Dropout rate. Applied in residual blocks of attn, mlp and inside the mlp.
+ 684         drop_path_rate: float = 0.0,
- 685 
+ 685         norm_layer: Union[Callable[..., nn.Module], str] = "LayerNorm",
- 686             return_interm_layers (bool): Whether to return intermediate layers (all global attention blocks).
+ 686         act_layer: Callable[..., nn.Module] = nn.GELU,
- 687             init_values: layer scale init, None for no layer scale.
+ 687         use_abs_pos: bool = True,
- 688 
+ 688         tile_abs_pos: bool = True,
~ 689             ln_pre (bool): If True, apply layer norm before transformer blocks. →         rel_pos_blocks: Union[Tuple[int, ...], bool] = (2, 5, 8, 11),
~ 690             ln_post (bool): If True, apply layer norm after transformer blocks. →         rel_pos_zero_init: bool = True,
- 691             bias_patch_embed (bool): bias in conv for patch embed?
+ 691         window_size: int = 14,
- 692             compile_mode (str): mode to compile the forward
+ 692         global_att_blocks: Tuple[int, ...] = (2, 5, 8, 11),
- 693         """
+ 693         use_rope: bool = False,
- 694         super().__init__()
+ 694         rope_pt_size: Optional[int] = None,
~ 695         self.pretrain_use_cls_token = pretrain_use_cls_token →         use_interp_rope: bool = False,
- 696 
+ 696         pretrain_img_size: int = 224,
~ 697         window_block_indexes = [i for i in range(depth) if i not in global_att_blocks] →         pretrain_use_cls_token: bool = True,
~ 698         self.full_attn_ids = list(global_att_blocks) →         retain_cls_token: bool = True,
- 699         self.rel_pos_blocks = [False] * depth
+ 699         dropout: float = 0.0,
~ 700         if isinstance(rel_pos_blocks, bool) and rel_pos_blocks: →         return_interm_layers: bool = False,
- 701             self.rel_pos_blocks = [True] * depth
+ 701         init_values: Optional[float] = None,  # for layerscale
- 702         else:
+ 702         ln_pre: bool = False,
- 703             for i in rel_pos_blocks:
+ 703         ln_post: bool = False,
~ 704                 self.rel_pos_blocks[i] = True →         bias_patch_embed: bool = True,
- 705 
+ 705         compile_mode: Optional[str] = None,
~ 706         self.retain_cls_token = retain_cls_token →         use_act_checkpoint: bool = True,
- 707         if self.retain_cls_token:
+ 707     ):
- 708             assert pretrain_use_cls_token
+ 708         """
- 709             assert (
+ 709         Args:
- 710                 len(window_block_indexes) == 0
+ 710             img_size (int): Input image size. Only relevant for rel pos or rope.
- 711             ), "windowing not supported with cls token"
+ 711             patch_size (int): Patch size.
- 712 
+ 712             in_chans (int): Number of input image channels.
~ 713             assert sum(self.rel_pos_blocks) == 0, "rel pos not supported with cls token" →             embed_dim (int): Patch embedding dimension.
- 714 
+ 714             depth (int): Depth of ViT.
- 715             scale = embed_dim**-0.5
+ 715             num_heads (int): Number of attention heads in each ViT block.
~ 716             self.class_embedding = nn.Parameter(scale * torch.randn(1, 1, embed_dim)) →             mlp_ratio (float): Ratio of mlp hidden dim to embedding dim.
- 717 
+ 717             qkv_bias (bool): If True, add a learnable bias to query, key, value.
~ 718         if isinstance(norm_layer, str): →             drop_path_rate (float): Stochastic depth rate.
- 719             norm_layer = partial(getattr(nn, norm_layer), eps=1e-5)
+ 719             norm_layer (nn.Module): Normalization layer.
- 720 
+ 720             act_layer (nn.Module): Activation layer.
- 721         self.patch_embed = PatchEmbed(
+ 721             use_abs_pos (bool): If True, use absolute positional embeddings.
- 722             kernel_size=(patch_size, patch_size),
+ 722             tile_abs_pos (bool): If True, tile absolute positional embeddings instead of interpolation.
- 723             stride=(patch_size, patch_size),
+ 723             rel_pos_blocks (list): Blocks which have rel pos embeddings.
- 724             in_chans=in_chans,
+ 724             rel_pos_zero_init (bool): If True, zero initialize relative positional parameters.
- 725             embed_dim=embed_dim,
+ 725             window_size (int): Window size for window attention blocks.
- 726             bias=bias_patch_embed,
+ 726             global_att_blocks (list): Indexes for blocks using global attention (other blocks use window attention).
- 727         )
+ 727             use_rope (bool): whether to use rope 2d (indep of rel_pos_blocks, as it can be used together).
- 728 
+ 728             rope_pt_size (int): size of rope in previous stage of training, needed for interpolation or tiling.
- 729         # Handle absolute positional embedding
+ 729             use_interp_rope: whether to interpolate (or extrapolate) rope to match target input size,
~ 730         self.tile_abs_pos = tile_abs_pos →                 expected to specify source size as rope_pt_size.
- 731         self.use_abs_pos = use_abs_pos
+ 731             use_act_checkpoint (bool): If True, use activation checkpointing.
~ 732         if self.tile_abs_pos: →             pretrain_img_size (int): input image size for pretraining models.
~ 733             assert self.use_abs_pos →             pretrain_use_cls_token (bool): If True, pretraining models use class token.
- 734 
+ 734             retain_cls_token: whether cls_token should be retained.
- 735         if self.use_abs_pos:
+ 735             dropout (float): Dropout rate. Applied in residual blocks of attn, mlp and inside the mlp.
- 736             # Initialize absolute positional embedding with pretrain image size.
+ 736 
~ 737             num_patches = (pretrain_img_size // patch_size) * ( →             return_interm_layers (bool): Whether to return intermediate layers (all global attention blocks).
- 738                 pretrain_img_size // patch_size
+ 738             init_values: layer scale init, None for no layer scale.
- 739             )
+ 739 
~ 740             num_positions = (num_patches + 1) if pretrain_use_cls_token else num_patches →             ln_pre (bool): If True, apply layer norm before transformer blocks.
~ 741             self.pos_embed = nn.Parameter(torch.zeros(1, num_positions, embed_dim)) →             ln_post (bool): If True, apply layer norm after transformer blocks.
- 742         else:
+ 742             bias_patch_embed (bool): bias in conv for patch embed?
- 743             self.pos_embed = None
+ 743             compile_mode (str): mode to compile the forward
- 744 
+ 744         """
- 745         # stochastic depth decay rule
+ 745         super().__init__()
~ 746         dpr = [x.item() for x in torch.linspace(0, drop_path_rate, depth)] →         self.pretrain_use_cls_token = pretrain_use_cls_token
~ 748         self.blocks = nn.ModuleList() →         window_block_indexes = [i for i in range(depth) if i not in global_att_blocks]
- 749         cur_stage = 1
+ 749         self.full_attn_ids = list(global_att_blocks)
- 750         for i in range(depth):
+ 750         self.rel_pos_blocks = [False] * depth
- 751             block = Block(
+ 751         if isinstance(rel_pos_blocks, bool) and rel_pos_blocks:
- 752                 dim=embed_dim,
+ 752             self.rel_pos_blocks = [True] * depth
- 753                 num_heads=num_heads,
+ 753         else:
- 754                 mlp_ratio=mlp_ratio,
+ 754             for i in rel_pos_blocks:
- 755                 qkv_bias=qkv_bias,
+ 755                 self.rel_pos_blocks[i] = True
- 756                 drop_path=dpr[i],
+ 756 
- 757                 norm_layer=norm_layer,
+ 757         self.retain_cls_token = retain_cls_token
- 758                 act_layer=act_layer,
+ 758         if self.retain_cls_token:
~ 759                 use_rel_pos=self.rel_pos_blocks[i], →             assert pretrain_use_cls_token
- 760                 rel_pos_zero_init=rel_pos_zero_init,
+ 760             assert (
~ 761                 window_size=window_size if i in window_block_indexes else 0, →                 len(window_block_indexes) == 0
- 762                 input_size=(img_size // patch_size, img_size // patch_size),
+ 762             ), "windowing not supported with cls token"
- 763                 use_rope=use_rope,
+ 763 
- 764                 rope_pt_size=(
+ 764             assert sum(self.rel_pos_blocks) == 0, "rel pos not supported with cls token"
- 765                     (window_size, window_size)
+ 765 
- 766                     if rope_pt_size is None
+ 766             scale = embed_dim**-0.5
- 767                     else (rope_pt_size, rope_pt_size)
+ 767             self.class_embedding = nn.Parameter(scale * torch.randn(1, 1, embed_dim))
- 768                 ),
+ 768 
- 769                 rope_interp=use_interp_rope,
+ 769         if isinstance(norm_layer, str):
- 770                 cls_token=self.retain_cls_token,
+ 770             norm_layer = partial(getattr(nn, norm_layer), eps=1e-5)
- 771                 dropout=dropout,
+ 771 
- 772                 init_values=init_values,
+ 772         self.patch_embed = PatchEmbed(
- 773             )
+ 773             kernel_size=(patch_size, patch_size),
- 774 
+ 774             stride=(patch_size, patch_size),
- 775             if i not in window_block_indexes:
+ 775             in_chans=in_chans,
- 776                 cur_stage += 1
+ 776             embed_dim=embed_dim,
- 777 
+ 777             bias=bias_patch_embed,
- 778             self.use_act_checkpoint = use_act_checkpoint
+ 778         )
- 780             self.blocks.append(block)
+ 780         # Handle absolute positional embedding
- 781 
+ 781         self.tile_abs_pos = tile_abs_pos
~ 782         self.return_interm_layers = return_interm_layers →         self.use_abs_pos = use_abs_pos
~ 783         self.channel_list = ( →         if self.tile_abs_pos:
- 784             [embed_dim] * len(self.full_attn_ids)
+ 784             assert self.use_abs_pos
- 785             if return_interm_layers
+ 785 
- 786             else [embed_dim]
+ 786         if self.use_abs_pos:
- 787         )
+ 787             # Initialize absolute positional embedding with pretrain image size.
- 788 
+ 788             num_patches = (pretrain_img_size // patch_size) * (
- 789         if self.pos_embed is not None:
+ 789                 pretrain_img_size // patch_size
- 790             trunc_normal_(self.pos_embed, std=0.02)
+ 790             )
- 791 
+ 791             num_positions = (num_patches + 1) if pretrain_use_cls_token else num_patches
~ 792         self.ln_pre = norm_layer(embed_dim) if ln_pre else nn.Identity() →             self.pos_embed = nn.Parameter(torch.zeros(1, num_positions, embed_dim))
- 793         self.ln_post = norm_layer(embed_dim) if ln_post else nn.Identity()
+ 793         else:
- 794 
+ 794             self.pos_embed = None
- 795         self.apply(self._init_weights)
+ 795 
- 796 
+ 796         # stochastic depth decay rule
- 797         if compile_mode is not None:
+ 797         dpr = [x.item() for x in torch.linspace(0, drop_path_rate, depth)]
- 798             self.forward = torch.compile(
+ 798 
- 799                 self.forward, mode=compile_mode, fullgraph=True
+ 799         self.blocks = nn.ModuleList()
- 800             )
+ 800         cur_stage = 1
~ 801             if self.use_act_checkpoint and self.training: →         for i in range(depth):
- 802                 torch._dynamo.config.optimize_ddp = False
+ 802             block = Block(
- 803 
+ 803                 dim=embed_dim,
- 804     def _init_weights(self, m: nn.Module) -> None:
+ 804                 num_heads=num_heads,
- 805         if isinstance(m, nn.Linear):
+ 805                 mlp_ratio=mlp_ratio,
- 806             trunc_normal_(m.weight, std=0.02)
+ 806                 qkv_bias=qkv_bias,
- 807             if isinstance(m, nn.Linear) and m.bias is not None:
+ 807                 drop_path=dpr[i],
- 808                 nn.init.constant_(m.bias, 0)
+ 808                 norm_layer=norm_layer,
- 809         elif isinstance(m, nn.LayerNorm):
+ 809                 act_layer=act_layer,
- 810             nn.init.constant_(m.bias, 0)
+ 810                 use_rel_pos=self.rel_pos_blocks[i],
- 811             nn.init.constant_(m.weight, 1.0)
+ 811                 rel_pos_zero_init=rel_pos_zero_init,
- 812 
+ 812                 window_size=window_size if i in window_block_indexes else 0,
- 813     def forward(self, x: torch.Tensor) -> List[torch.Tensor]:
+ 813                 input_size=(img_size // patch_size, img_size // patch_size),
- 814         x = self.patch_embed(x)
+ 814                 use_rope=use_rope,
- 815         h, w = x.shape[1], x.shape[2]
+ 815                 rope_pt_size=(
- 816 
+ 816                     (window_size, window_size)
- 817         s = 0
+ 817                     if rope_pt_size is None
- 818         if self.retain_cls_token:
+ 818                     else (rope_pt_size, rope_pt_size)
- 819             # If cls_token is retained, we don't
+ 819                 ),
- 820             # maintain spatial shape
+ 820                 rope_interp=use_interp_rope,
~ 821             x = torch.cat([self.class_embedding, x.flatten(1, 2)], dim=1) →                 cls_token=self.retain_cls_token,
- 822             s = 1
+ 822                 dropout=dropout,
- 823 
+ 823                 init_values=init_values,
- 824         if self.pos_embed is not None:
+ 824             )
- 825             x = x + get_abs_pos(
+ 825 
- 826                 self.pos_embed,
+ 826             if i not in window_block_indexes:
- 827                 self.pretrain_use_cls_token,
+ 827                 cur_stage += 1
- 828                 (h, w),
+ 828 
~ 829                 self.retain_cls_token, →             self.use_act_checkpoint = use_act_checkpoint
- 830                 tiling=self.tile_abs_pos,
+ 830 
- 831             )
+ 831             self.blocks.append(block)
- 833         x = self.ln_pre(x)
+ 833         self.return_interm_layers = return_interm_layers
- 834 
+ 834         self.channel_list = (
- 835         outputs = []
+ 835             [embed_dim] * len(self.full_attn_ids)
~ 836         for i, blk in enumerate(self.blocks): →             if return_interm_layers
- 837             if self.use_act_checkpoint and self.training:
+ 837             else [embed_dim]
- 838                 x = checkpoint.checkpoint(blk, x, use_reentrant=False)
+ 838         )
- 839             else:
+ 839 
- 840                 x = blk(x)
+ 840         if self.pos_embed is not None:
~ 841             if (i == self.full_attn_ids[-1]) or ( →             trunc_normal_(self.pos_embed, std=0.02)
- 842                 self.return_interm_layers and i in self.full_attn_ids
+ 842 
- 843             ):
+ 843         self.ln_pre = norm_layer(embed_dim) if ln_pre else nn.Identity()
- 844                 if i == self.full_attn_ids[-1]:
+ 844         self.ln_post = norm_layer(embed_dim) if ln_post else nn.Identity()
- 845                     x = self.ln_post(x)
+ 845 
- 846 
+ 846         self.apply(self._init_weights)
- 847                 feats = x[:, s:]
+ 847 
- 848                 if feats.ndim == 4:
+ 848         if compile_mode is not None:
- 849                     feats = feats.permute(0, 3, 1, 2)
+ 849             self.forward = torch.compile(
- 850                 else:
+ 850                 self.forward, mode=compile_mode, fullgraph=True
- 851                     assert feats.ndim == 3
+ 851             )
- 852                     h = w = math.sqrt(feats.shape[1])
+ 852             if self.use_act_checkpoint and self.training:
- 853                     feats = feats.reshape(
+ 853                 torch._dynamo.config.optimize_ddp = False
- 854                         feats.shape[0], h, w, feats.shape[-1]
+ 854 
- 855                     ).permute(0, 3, 1, 2)
+ 855     def _init_weights(self, m: nn.Module) -> None:
- 856 
+ 856         if isinstance(m, nn.Linear):
- 857                 outputs.append(feats)
+ 857             trunc_normal_(m.weight, std=0.02)
- 858 
+ 858             if isinstance(m, nn.Linear) and m.bias is not None:
- 859         return outputs
+ 859                 nn.init.constant_(m.bias, 0)
- 860 
+ 860         elif isinstance(m, nn.LayerNorm):
- 861     def get_layer_id(self, layer_name: str) -> int:
+ 861             nn.init.constant_(m.bias, 0)
- 862         # https://github.com/microsoft/unilm/blob/master/beit/optim_factory.py#L33
+ 862             nn.init.constant_(m.weight, 1.0)
- 863         num_layers = self.get_num_layers()
+ 863 
- 864 
+ 864     def forward(self, x: torch.Tensor) -> List[torch.Tensor]:
- 865         if layer_name.find("rel_pos") != -1:
+ 865         x = self.patch_embed(x)
- 866             return num_layers + 1
+ 866         h, w = x.shape[1], x.shape[2]
- 867         elif layer_name.find("ln_pre") != -1:
+ 867 
- 868             return 0
+ 868         s = 0
~ 869         elif layer_name.find("pos_embed") != -1 or layer_name.find("cls_token") != -1: →         if self.retain_cls_token:
- 870             return 0
+ 870             # If cls_token is retained, we don't
- 871         elif layer_name.find("patch_embed") != -1:
+ 871             # maintain spatial shape
- 872             return 0
+ 872             x = torch.cat([self.class_embedding, x.flatten(1, 2)], dim=1)
- 873         elif layer_name.find("blocks") != -1:
+ 873             s = 1
- 874             return int(layer_name.split("blocks")[1].split(".")[1]) + 1
+ 874 
- 875         else:
+ 875         if self.pos_embed is not None:
- 876             return num_layers + 1
+ 876             x = x + get_abs_pos(
- 877 
+ 877                 self.pos_embed,
- 878     def get_num_layers(self) -> int:
+ 878                 self.pretrain_use_cls_token,
- 879         return len(self.blocks)
+ 879                 (h, w),
+ 880                 self.retain_cls_token,
+ 881                 tiling=self.tile_abs_pos,
+ 882             )
+ 883 
+ 884         x = self.ln_pre(x)
+ 885 
+ 886         outputs = []
+ 887         for i, blk in enumerate(self.blocks):
+ 888             if self.use_act_checkpoint and self.training:
+ 889                 x = checkpoint.checkpoint(blk, x, use_reentrant=False)
+ 890             else:
+ 891                 x = blk(x)
+ 892             if (i == self.full_attn_ids[-1]) or (
+ 893                 self.return_interm_layers and i in self.full_attn_ids
+ 894             ):
+ 895                 if i == self.full_attn_ids[-1]:
+ 896                     x = self.ln_post(x)
+ 897 
+ 898                 feats = x[:, s:]
+ 899                 if feats.ndim == 4:
+ 900                     feats = feats.permute(0, 3, 1, 2)
+ 901                 else:
+ 902                     assert feats.ndim == 3
+ 903                     h = w = math.sqrt(feats.shape[1])
+ 904                     feats = feats.reshape(
+ 905                         feats.shape[0], h, w, feats.shape[-1]
+ 906                     ).permute(0, 3, 1, 2)
+ 907 
+ 908                 outputs.append(feats)
+ 909 
+ 910         return outputs
+ 911 
+ 912     def get_layer_id(self, layer_name: str) -> int:
+ 913         # https://github.com/microsoft/unilm/blob/master/beit/optim_factory.py#L33
+ 914         num_layers = self.get_num_layers()
+ 915 
+ 916         if layer_name.find("rel_pos") != -1:
+ 917             return num_layers + 1
+ 918         elif layer_name.find("ln_pre") != -1:
+ 919             return 0
+ 920         elif layer_name.find("pos_embed") != -1 or layer_name.find("cls_token") != -1:
+ 921             return 0
+ 922         elif layer_name.find("patch_embed") != -1:
+ 923             return 0
+ 924         elif layer_name.find("blocks") != -1:
+ 925             return int(layer_name.split("blocks")[1].split(".")[1]) + 1
+ 926         else:
+ 927             return num_layers + 1
+ 928 
+ 929     def get_num_layers(self) -> int:
+ 930         return len(self.blocks)
